# aura-local-file-agent
Aura - A local-first autonomous AI file organization agent. Monitors your file system, reads file contents using local LLMs (Ollama), intelligently categorizes and renames files, auto-organizes them into appropriate folders, and maintains a searchable semantic database using ChromaDB. Zero cloud dependency, 100% privacy-first.
